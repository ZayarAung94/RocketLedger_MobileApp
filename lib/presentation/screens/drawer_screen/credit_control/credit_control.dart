import 'package:flutter/material.dart';

class CreditControl extends StatelessWidget {
  const CreditControl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
