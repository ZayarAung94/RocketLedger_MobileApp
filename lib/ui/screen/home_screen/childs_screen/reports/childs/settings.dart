import 'package:flutter/material.dart';

class GameSettingsTab extends StatelessWidget {
  const GameSettingsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
